# Backend

FastAPI backend for the Algebra open lesson site.

## Env
- Copy `.env.example` to `.env`
- Set `OPENAI_API_KEY` if you want AI/OCR

## Run (dev)
```
uvicorn app.main:app --reload --port 8000
```
